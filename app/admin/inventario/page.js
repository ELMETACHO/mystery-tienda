import InventarioApp from "./InventarioApp";

export const metadata = {
  title: "Inventario — Mystery",
  robots: { index: false, follow: false },
};

export default function InventarioPage() {
  return <InventarioApp />;
}
