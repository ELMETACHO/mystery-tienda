import FinanzasApp from "./FinanzasApp";

export const metadata = {
  title: "Finanzas — Mystery",
  robots: { index: false, follow: false },
};

export default function FinanzasPage() {
  return <FinanzasApp />;
}
