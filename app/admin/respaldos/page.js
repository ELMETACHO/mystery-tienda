import RespaldosApp from "./RespaldosApp";

export const metadata = {
  title: "Respaldos de pedidos — Mystery",
  robots: { index: false, follow: false },
};

export default function RespaldosPage() {
  return <RespaldosApp />;
}
