import ReporteApp from "./ReporteApp";

export const metadata = {
  title: "Reporte financiero — Mystery",
  robots: { index: false, follow: false },
};

export default function ReportePage() {
  return <ReporteApp />;
}
